# ===================================
# BookConnect Environment Variables
# ===================================

# Node Environment (development or production)
NODE_ENV=development

# Server Configuration
PORT=3000

# MongoDB Configuration
# For development: mongodb://localhost:27017/book_connect
# For production: mongodb+srv://username:password@cluster.mongodb.net/book_connect
# Get MongoDB Atlas free at: https://www.mongodb.com/cloud/atlas
MONGODB_URI=mongodb://localhost:27017/book_connect

# Redis Configuration (Optional but Recommended)
# For development: redis://localhost:6379
# For production: redis://:password@host:port
# Get Redis Cloud free at: https://redis.com/try-free/
# Or Upstash: https://upstash.com/
REDIS_URL=redis://localhost:6379

# Session Secret (REQUIRED - use a strong random string)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Admin Configuration
# Email that should have admin access
ADMIN_EMAIL=admin@bookconnect.com

# Host Credentials (Optional - defaults used if not set)
HOST_EMAIL=host@bookconnect.com
HOST_PASSWORD=host123

