# MongoDB Configuration
# For local: mongodb://localhost:27017/book_connect
# For cloud (MongoDB Atlas): mongodb+srv://username:password@cluster.mongodb.net/book_connect
MONGODB_URI=mongodb://localhost:27017/book_connect

# Redis Configuration
# For local: redis://localhost:6379
# For cloud (Redis Cloud/Upstash): redis://username:password@host:port
# Optional - app works without Redis (uses MongoDB sessions as fallback)
REDIS_URL=redis://localhost:6379

# Session Secret (REQUIRED - use a strong random string in production)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Server Port (usually set by hosting platform)
PORT=3000

# Admin Configuration (Optional)
# Email that should have admin access
ADMIN_EMAIL=admin@bookconnect.com

# Host Credentials (Optional - defaults used if not set)
HOST_EMAIL=host@bookconnect.com
HOST_PASSWORD=host123

# Node Environment
NODE_ENV=production
